import { StyleSheet, Text, View } from "react-native";
import React from "react";
import MessageArea from "@/src/components/message";

const Message = () => {
  return <MessageArea />;
};

export default Message;

const styles = StyleSheet.create({});
